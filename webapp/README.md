# QA Assist Web App

Static UI for session playback. Serve this folder with any static server.

Example:
```
python -m http.server 5173
```

Set API URL to the Phoenix backend base (default http://localhost:4000/api) and enter a device id.
